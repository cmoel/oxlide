<!-- fixture: four-cell 2x2 grid of prose cells -->

	Builds run green on every merge to main.

	Lint catches stylistic drift before review.

	Types prove module invariants at compile time.

	Tests guard regressions on every commit.
